type Props = {
    scale?: number;
    image?: {
        src: string;
        alt: string;
    };
    borderRadius?: number;
};
export default function ImageScaleOnScroll({ scale, image, borderRadius }: Props): JSX.Element;
export {};
